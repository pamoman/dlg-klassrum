build:
  nodes:
    functional-tests:
      environment:
        selenium: true
    coverage:
      tests:
        override:
          - command: npm test

filter:
    excluded_paths:
        - test/
